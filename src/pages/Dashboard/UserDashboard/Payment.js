import { Typography } from '@mui/material';
import React from 'react';

const Payment = () => {
	return (
		<div>
			<Typography sx={{ minHeight: '30vh' }} variant='h5'>
				Payment system is coming soon
			</Typography>
		</div>
	);
};

export default Payment;
